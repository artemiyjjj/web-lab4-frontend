
const svgWidth = 400;
const svgHeight = 400;
const svgOffset = 10;

export const d3Consts = {
    width: svgWidth,
    height: svgHeight,
    offset: svgOffset,
    widthWithOffset: svgWidth + svgOffset * 2,
    heightWithOffset: svgHeight + svgOffset * 2,
}

export const formInputs = {
    xInput: "xInput",
    yInput: "yInput",
    rInput: "rInput",
}
